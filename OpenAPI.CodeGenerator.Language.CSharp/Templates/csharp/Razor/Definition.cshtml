@using System.Linq
@using OpenAPI.CodeGenerator.Common.Extensions
@model OpenAPI.CodeGenerator.OpenAPI.Items.APIDefinition

@foreach (var controller in Model.OpenApiDocument.GetControllers())
{
<text>
    public class </text>@controller.Name<text>Controller
    {
</text>
	foreach (var pathItem in controller.OpenApiPaths)
	{
		foreach(var operationItem in pathItem.Value.Operations)
		{
			<text>
				public void </text>@operationItem.OperationId<text>Action
				{
				}
			</text>
	    }
	<text>
    }
</text>
}
